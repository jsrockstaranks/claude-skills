# Example Output

<!--
  This file is proof that the skill works. It shows exactly what Claude
  produced when given the prompt in input.md.

  Rules:
  - Copy-paste REAL output from Claude, don't write it by hand
  - Run the skill at least 3 times and use the best result
  - Include the FULL output — no truncating with "..."
  - If the output is very long (>300 lines of code), you may trim to the
    most illustrative parts, but add a comment: "// [truncated — see full
    output in example/output-full.md]" and include the full file too

  Structure:
  1. Summary paragraph (what Claude decided and why)
  2. Each output file in a labeled code block
  3. Usage section
  4. Props/API table (if applicable)
-->

> [Paste Claude's summary paragraph here — the one-paragraph overview it
> generates at the start of its response, describing what was built and
> any key decisions made.]

---

## [OutputFile1.ext]

```[language]
// ─── [OutputFile1.ext] ──────────────────────────────────────────────────────
[Full file content here]
```

---

## [OutputFile2.ext]

```[language]
// ─── [OutputFile2.ext] ──────────────────────────────────────────────────────
[Full file content here]
```

---

## Usage

```[language]
[Copy-pasteable usage example]
```

---

<!--
  Optional: include the props/API table if the skill generates something
  with a public interface (component, function, CLI tool, etc.)
-->

## API / Props

| Name | Type | Default | Description |
|---|---|---|---|
| `[name]` | `[type]` | `[default]` | [description] |
